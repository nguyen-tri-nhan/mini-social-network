apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-consumer
  namespace: social
spec:
  replicas: 1
  selector:
    matchLabels: { app: user-consumer }
  template:
    metadata:
      labels: { app: user-consumer }
    spec:
      containers:
        - name: user-consumer
          image: nhan/user-consumer:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://lgtm:4317
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/social
            - name: REDIS_URL
              value: redis://redis:6379
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
          readinessProbe:
            httpGet: { path: /q/health/ready, port: 8080 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /q/health/live, port: 8080 }
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "192Mi"
              cpu: "100m"
            limits:
              memory: "384Mi"
              cpu: "300m"
